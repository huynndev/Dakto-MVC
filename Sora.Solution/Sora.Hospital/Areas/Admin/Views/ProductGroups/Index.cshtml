@using Sora.Services.ViewModels
@using Sora.Common.Enums
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutSimpleAdmin.cshtml";
    var models = (List<ProductGroupViewModel>)Model;
    var isSuccess = ViewBag.Success;
    var Message = ViewBag.Message;
    var counter = 1;
}
<!-- Page-header start -->
<div class="page-header horizontal-layout-icon">
    <div class="row align-items-end">
        <div class="col-lg-8">
            <div class="page-header-title">
                <div class="d-inline">
                    <h4>Quản lý nhóm sản phẩm</h4>
                    <span>Danh sách nhóm sản phẩm. Bạn có thể sửa, thêm mới và xóa</span>
                </div>
            </div>
        </div>
        <div class="col-lg-4">
            <div class="page-header-breadcrumb">
                <ul class="breadcrumb-title">
                    <li class="breadcrumb-item">
                        <a href="~/Admin">
                            <i class="icofont icofont-home"></i>
                        </a>
                    </li>
                    <li class="breadcrumb-item">
                        <a href="@Url.Action("Index","ProductGroups")">Nhóm sản phẩm</a>
                    </li>
                    <li class="breadcrumb-item">
                        <a href="#!">Danh sách</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>

<!-- Page-header end -->
<div class="page-body">
    <div class="row">
        <div class="col-sm-12">
            <!-- Zero config.table start -->
            <div class="card">
                <div class="card-header">
                    <h5>Danh sách </h5>
                    <a href="@Url.Action("Create", "ProductGroups")" class="btn btn-mid btn-success">Thêm mới nhóm sản phẩm</a>
                    <div class="card-header-right">
                        <ul class="list-unstyled card-option">
                            <li><i class="feather icon-maximize full-card"></i></li>
                            <li><i class="feather icon-minus minimize-card"></i></li>
                        </ul>
                    </div>
                </div>
                <div class="card-block">
                    <div class="dt-responsive table-responsive">
                        <table id="listData" class="table table-striped table-bordered nowrap">
                            <thead>
                                <tr>
                                    <th>Thứ tự</th>
                                    <th>Nhóm sản phẩm</th>
                                    <th>Loại</th>
                                    <th>Thuộc nhóm sản phẩm</th>
                                    <th>Số lượng sản phẩm</th>
                                    <th>Thao tác</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (ProductGroupViewModel item in models)
                                {
                                    <tr>
                                        <td>@(counter++) </td>
                                        <td>
                                            <strong>@item.ICProductGroupName</strong><br />
                                        </td>
                                        <td>
                                            @if (item.ICProductGroupType.Equals(SpecialistsType.HealthServices.ToString()))
                                            {
                                                <span>Dịch vụ y tế</span>
                                            }
                                            else if (item.ICProductGroupType.Equals(SpecialistsType.Maternity.ToString()))
                                            {
                                                <span>Thai sản</span>
                                            }
                                        </td>
                                        <td>
                                            @(item.Parent != null ? item.Parent.ICProductGroupName : "//")
                                        </td>
                                        <td>
                                            @item.TotalProduct Sản phẩm &nbsp;
                                            @Html.ActionLink("Xem", "Index", "Products", new { groupId = item.ICProductGroupID }, new { @class = "btn btn-primary btn-sm waves-effect waves-light" })
                                        </td>
                                        <td align="center">
                                            <div class="btn-group  " role="group" data-toggle="tooltip" data-placement="top" title="" data-original-title=".btn-xlg">

                                                @using (Html.BeginForm("Delete", "Specialists",
                                                    new { id = item.ICProductGroupID },
                                                    FormMethod.Post,
                                                    new { id = item.ICProductGroupID })
                                                )
                                                {
                                                    @Html.HttpMethodOverride(HttpVerbs.Delete){
                                                        @Html.ActionLink("Sửa", "Detail", "ProductGroups", new { id = item.ICProductGroupID }, new { @class = "btn btn-primary btn-sm waves-effect waves-light" })
                                                        <input class="btn btn-primary btn-sm waves-effect waves-light" type="submit" value="Xóa" onclick="return confirm('Bạn chắc chắn có muốn xóa!');" />
                                                    }
                                                }
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>

                        </table>
                    </div>
                </div>
            </div>
            <!-- Zero config.table end -->
        </div>
    </div>
</div>
<!-- Page-body end -->
@section scripts {
    @if (isSuccess != null)
    {
        if (isSuccess)
        {
            <script>
                toastr.success('@Message', 'Thành công');
            </script>
        }
        else
        {
            <script>
                toastr.error('@Message', 'Thất bại',{timeOut: 8000});
            </script>
        }
    }
}
